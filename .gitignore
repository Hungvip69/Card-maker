# Dependencies
node_modules/

# Database (WAL mode also creates -wal/-shm)
cards.db
cards.db-wal
cards.db-shm
*.db
*.db-wal
*.db-shm

# Environment & secrets (KLIPY_KEY, etc.) — NEVER commit
.env
.env.*
!.env.example

# Logs & temp
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Internal working files (notes, reviews, ideas) — keep out of the repo
.d_*.txt
_review_*.txt
IDEA.md
FEATURE_IDEAS.md
FEATURE_LOG.md
